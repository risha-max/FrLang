definir fonction contient_doublon(Rangee nums) {
    soit Sac vu = nouveau Sac();
    pourchaque i dans range(nums.taille()) {
        soit nombre val = nums.element(i);
        si vu.contient(val) {
            retourne vrai;
        }
        vu.ajouter(val);
    }
    retourne faux;
} retourne logique

afficher contient_doublon({1, 2, 3, 1});
afficher contient_doublon({1, 2, 3, 4});
afficher contient_doublon({1, 1, 1, 3, 3, 4, 3, 2, 4, 2});
